# ansible/

This directory will contain all Ansible playbooks, roles, and inventory files for the models-server automation.

- Add your main playbook as `site.yml`.
- Place inventory files (e.g., `inventory`) here.
- Organize roles under `roles/` if needed.

See the main README for bootstrapping instructions.
